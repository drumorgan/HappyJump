{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "if": "Bash(git commit:*)",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"REMINDER: Check if CLAUDE.md needs to be updated to reflect these changes. Review what was changed and determine if any section of CLAUDE.md should be added or updated.\"}}'",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "Bash",
        "if": "Bash(git push:*)",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"STANDING ORDER REMINDER: You just pushed a branch. Per CLAUDE.md you MUST now (1) create a Pull Request via the GitHub API and (2) merge it via the GitHub API (squash). Do NOT stop here. Do NOT ask the user for permission — blanket approval has already been granted for this repo. The only exception is direct pushes to main for supabase/functions/gateway/index.ts.\"}}'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
